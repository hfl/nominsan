<%= form_with(model: alternative_title, local: true) do |form| %>
  <% if alternative_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternative_title.errors.count, "error") %> prohibited this alternative_title from being saved:</h2>

      <ul>
      <% alternative_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title_id %>
    <%= form.text_field :title_id %>
  </div>

  <div class="field">
    <%= form.label :alternative_title %>
    <%= form.text_field :alternative_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
