<%= form_with(model: coverage, local: true) do |form| %>
  <% if coverage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coverage.errors.count, "error") %> prohibited this coverage from being saved:</h2>

      <ul>
      <% coverage.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :literature_id %>
    <%= form.text_field :literature_id %>
  </div>

  <div class="field">
    <%= form.label :coverage_type_id %>
    <%= form.text_field :coverage_type_id %>
  </div>

  <div class="field">
    <%= form.label :coverage %>
    <%= form.text_field :coverage %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
