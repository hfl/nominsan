<p id="notice"><%= notice %></p>

<h1>Rights</h1>

<table>
  <thead>
    <tr>
      <th>Literature</th>
      <th>Right type</th>
      <th>Right</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rights.each do |right| %>
      <tr>
        <td><%= right.literature %></td>
        <td><%= right.right_type %></td>
        <td><%= right.right %></td>
        <td><%= link_to 'Show', right %></td>
        <td><%= link_to 'Edit', edit_right_path(right) %></td>
        <td><%= link_to 'Destroy', right, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Right', new_right_path %>
